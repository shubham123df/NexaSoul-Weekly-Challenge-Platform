PORT=5000

# Local MongoDB (default for development)
MONGODB_URI=mongodb://127.0.0.1:27017/nexasoul-quiz

# MongoDB Atlas (production)
# Replace <user>, <password>, and <cluster> with your Atlas credentials.
# MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/nexasoul-quiz?retryWrites=true&w=majority&appName=<appName>

CLIENT_URL=http://localhost:5173
ADMIN_SECRET=nexasoul-admin-2026
